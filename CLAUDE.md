# java-springboot-multitenant-template

QCP multitenant Spring Boot service template (database-per-tenant). All project conventions and hard rules:

@AGENTS.md

Full QCP standards documents live in `docs/standards/` — consult them (especially `multi-tenancy.md`)
before generating controllers, services, DTOs, migrations, multitenancy or Docker changes.
