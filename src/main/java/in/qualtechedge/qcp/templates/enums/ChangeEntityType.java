package in.qualtechedge.qcp.templates.enums;

/** Kind of entity behind a checker-inbox item ({@code v_checker_inbox.entity_type}). */
public enum ChangeEntityType {
    process,
    template,
    role,
    user,
    storage,
    database,
    apiConfig
}
