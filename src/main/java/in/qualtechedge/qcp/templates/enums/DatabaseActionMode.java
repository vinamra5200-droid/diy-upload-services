package in.qualtechedge.qcp.templates.enums;

/** How a template's database post-load action picks its target when {@code type = database}. */
public enum DatabaseActionMode {
    useExisting,
    custom
}
