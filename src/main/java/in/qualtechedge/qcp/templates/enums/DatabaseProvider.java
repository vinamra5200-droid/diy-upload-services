package in.qualtechedge.qcp.templates.enums;

/** Target RDBMS for a database connection or a template's custom database post-load action. */
public enum DatabaseProvider {
    POSTGRES,
    MYSQL,
    SQL_SERVER,
    ORACLE
}
