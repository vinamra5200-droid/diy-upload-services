package in.qualtechedge.qcp.templates.enums;

/** Object storage provider for a pending-submission interim storage connection. */
public enum InterimStoreProvider {
    AWS_S3,
    AZURE_BLOB,
    GCS,
    ON_PREM
}
