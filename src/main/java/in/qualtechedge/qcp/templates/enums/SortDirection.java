package in.qualtechedge.qcp.templates.enums;

/** Sort direction for a {@code template_sort_fields} entry, used when {@link RowOrderMode#sortByKey}. */
public enum SortDirection {
    asc,
    desc
}
