package in.qualtechedge.qcp.templates.enums;

/** Comparison used by a {@code TRANSACTION} validation rule's split-sum check. */
public enum SumCompareOperator {
    lt,
    lte,
    gt,
    gte,
    eq
}
