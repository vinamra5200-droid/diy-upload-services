package in.qualtechedge.qcp.templates.enums;

/** Kafka topic {@code cleanup.policy} a queue config can pick (V1_4_0 — queue_configs.topic_cleanup_policy). */
public enum TopicCleanupPolicy {
    delete,
    compact
}
