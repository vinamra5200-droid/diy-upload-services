package in.qualtechedge.qcp.templates.enums;

/** Supported upload file formats per template ({@code template_upload_formats.format_key}). */
public enum UploadFormatKey {
    xlsx,
    csv,
    json
}
